name: Free Mac Terminal Cloud
on: 
  workflow_dispatch: 

jobs:
  launch-terminal:
    runs-on: macos-latest
    steps:
      - name: Open Direct Web Browser Terminal
        uses: mxschmitt/action-tmate@v3
